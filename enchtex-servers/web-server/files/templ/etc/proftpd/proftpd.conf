# This is a basic ProFTPD configuration file (rename it to
# 'proftpd.conf' for actual use. It establishes a single server
# and a single anonymous login. It assumes that you have a user/group
# "nobody" and "ftp" for normal operation and anonymous access.

ServerName			"ftp server"
ServerType			inetd
#ServerType			standalone
ServerIdent		on "nothing"
UseReverseDNS 		off
IdentLookups            off
DefaultServer		on
RequireValidShell	no
AuthPAM			on
AuthPAMConfig		ftp
# Port 21 is the standard FTP port.
Port				21

# Umask 022 is a good standard umask to prevent new dirs and files
# from being group and world writable.
Umask				022

# To prevent DoS attacks, set the maximum number of child processes
# to 30. If you need to allow more than 30 concurrent connections
# at once, simply increase this value. Note that this ONLY works
# in standalone mode, in inetd mode you should use an inetd server
# that allows you to limit the maximum number of processes per service
# (such as xinetd).
MaxInstances		5
MaxClients 10 "Only 10 connections allowed"
MaxClientsPerHost 5 "You have already logged on once"
MaxClientsPerUser 5 "You have already logged on once"
TimeoutStalled 3600
TimeoutNoTransfer 3600
TimeoutLogin 60


# Set the user and group under which the server will run.
User				proftpd
Group				proftpd

# Normally, we want files to be overwriteable.
<Directory />
	AllowOverwrite		yes
</Directory>

DefaultRoot ~
TransferLog /var/log/transferlog
DenyFilter \*.*/
